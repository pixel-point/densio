{
  "name": "ffmpeg-api",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "check": "turbo run typecheck test && pnpm lint && pnpm format:check",
    "dev": "turbo run dev --filter=@ffmpeg-api/api",
    "format": "oxfmt --disable-nested-config --write .",
    "format:check": "oxfmt --check --disable-nested-config .",
    "lint": "oxlint --disable-nested-config .",
    "lint:fix": "oxlint --disable-nested-config . --fix",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@types/archiver": "^8.0.0",
    "@types/node": "^25.0.0",
    "@typescript/native-preview": "^7.0.0-dev.20260703.1",
    "drizzle-kit": "1.0.0-rc.4",
    "esbuild": "^0.28.1",
    "oxfmt": "^0.57.0",
    "oxlint": "^1.7.0",
    "tsx": "^4.21.0",
    "turbo": "^2.8.0",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=22.18"
  },
  "packageManager": "pnpm@11.7.0"
}

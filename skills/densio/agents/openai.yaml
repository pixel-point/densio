interface:
  display_name: "Densio"
  short_description: "Agent-first video optimization through the densio CLI"
  default_prompt: "Use $densio to optimize, inspect, or extract media with resumable jobs and JSON output."
